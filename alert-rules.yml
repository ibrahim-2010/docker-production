groups:
  - name: flask-app-alerts
    rules:
      - alert: FlaskAppDown
        expr: up{job="flask-app"} == 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "Flask app is down"
          description: "The Flask application has been unreachable for more than 30 seconds."